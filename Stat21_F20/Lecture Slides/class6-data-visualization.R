## Class 6 - Visualizing data exercise 
install.packages("datasets")
library("datasets")


## The mtcars data set is taken from the 1974 Motor Trend magazine and covers fuel consumption and other aspects of automobile design for 32 different cars.  
head(mtcars)
class(mtcars)

## The different plot types we have covered so far include:
## geom_histogram(nbins=), geom_bar(), geom_point(), and geom_boxplot()

## Part 1 - Create a scatterplot of miles per gallon and weight. Label the axes clearly so that the reader can understand the units.  
#ggplot(, aes()) +
#                + 
#  labs() + 
#  xlab() + 
#  ylab()


## Part 2 - Create a bar chart for the variable that indicates transmission type. 
#ggplot(, aes()) +
#                + 
#  labs() + 
#  xlab() + 
#  ylab()


## Part 3 - Create a histogram for the variable miles per gallon.
#ggplot(, aes()) +
#                + 
#  labs() + 
#  xlab() + 
#  ylab()


## Part 4 - Create a box plot for the variable miles per gallon over each of the two different transmission types. 
#ggplot(, aes()) +
#                + 
#  labs() + 
#  xlab() + 
#  ylab()